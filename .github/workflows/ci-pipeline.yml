name: Continuous Integration

on:
  schedule:
    - cron: '47 11 * * *'

jobs:
  build:
    name: build and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install node_modules
        run: npm install
      - name: test
        run: npm run test:ci
      - name: build
        run: npm run build